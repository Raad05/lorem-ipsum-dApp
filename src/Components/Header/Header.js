import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <nav className='header'>
            <h2>Header connected</h2>
        </nav>
    );
};

export default Header;